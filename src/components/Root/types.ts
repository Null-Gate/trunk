export type NewFeedItem = {
  username: { String: string };
  pkg_details: string;
  package_name: string;
  id: { tb: string; String: string };
  formattedDate: string;
  date_to_go: string;
  userinfo: { id: { String: string } };
  to_where: string;
  from_where: string;
  car_info: { id: { String: string } };
};
